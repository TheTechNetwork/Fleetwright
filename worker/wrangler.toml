# The coordinator, on Cloudflare.
#
#   cd worker && npx wrangler deploy
#
# §4's reasoning, unchanged: you own no port, no VM, no cert and no tunnel
# daemon. Hosts open an outbound WebSocket and a Durable Object pins it; the
# phone speaks HTTPS to the same origin. This is not a tunnel — nothing is
# aimed at your boxes, they are ordinary clients.

name = "agent-fleet-coordinator"
main = "src/worker.js"
compatibility_date = "2026-01-15"

# nodejs_als, NOT nodejs_compat, and the difference is the whole of the note
# below. The Sentry SDK needs AsyncLocalStorage to associate an error with the
# request that caused it; `nodejs_als` provides exactly that one API, while
# `nodejs_compat` pulls in the whole polyfill surface this file spent a
# paragraph declining. The objection was cold start, and it still stands — this
# is the smallest flag that answers it.
compatibility_flags = ["nodejs_als"]

# nodejs_compat is not enabled, and that is a choice rather than a limitation.
#
# Workers runs npm packages and, with this flag, most of the node: built-ins —
# so nothing here is off-limits. What the flag costs is bundle size and cold
# start, and §4 picked this platform for a requirement measured in exactly that:
# "Hey Siri, resume bigjob" from cellular, on a cold radio, in under a second.
#
# The code the Worker imports — coordinator/core.js and push.js, and what they
# import in turn — happens to need only fetch and crypto.subtle, both of which
# are standard here and in Node. So the flag buys nothing today.
#
# If something genuinely needs a node: built-in or an npm package that pulls
# one in, add:
#
#     compatibility_flags = ["nodejs_compat", "nodejs_als"]
#
# UPDATE, 1 Sep 2026: `nodejs_als` is now set, for Sentry. That is the narrow
# flag — one API, AsyncLocalStorage — and the reasoning above is unchanged for
# the broad one. What error reporting DOES cost is bundle: 145 KB minified
# before it and 327 KB after, measured rather than estimated. If a cold start
# on cellular ever misses the second this file is written around, the SDK is
# the first thing to try removing, and SENTRY_DSN being unset already disables
# it without a deploy of code.
#
# and it will work. There is no rule being enforced; `wrangler deploy --dry-run`
# in CI simply reports the truth either way.

# The origin a host pins and a phone talks to. custom_domain = true makes
# Cloudflare create and manage the DNS record and the certificate for it, which
# is the difference between this and a plain route — there is nothing to point
# anywhere by hand.
#
# The hostname matters more here than it looks: §5 has the host agent PIN the
# coordinator origin, so changing it means editing every host's
# /etc/agent-fleet-sidecar.env. It is not a value to churn.
#
# ONE DOMAIN, AND THE DEMO IS NO LONGER ON IT. `fleetdemo` used to be a second
# custom domain on this same Worker, answered above the host routes so a request
# there never reached enrolment, a websocket, sign-in, or the Durable Object.
# That was true, and it was an argument about the ORDER of branches inside a
# bundle that also holds the App's client secret and the APNs key.
#
# It is a separate script now — `wrangler.demo.toml`, which has no Durable
# Object binding and no secrets. See docs/coordinator-deploy.md.
#
# `custom_domain = true` makes wrangler create the DNS record and the domain
# binding on deploy, so there is nothing to point anywhere by hand.
routes = [
  { pattern = "fleet.thetech.network", custom_domain = true }
]

# --- plain configuration, deployed with the code -----------------------------
[vars]
# WHERE THE PRODUCT PAGE IS, and this Worker does not serve it.
#
# /docs on the coordinator is a 302 to this address and nothing else. A
# coordinator is the thing holding the fleet; it should not also be the surface
# a stranger loads HTML from. The page lives on the demo Worker, which has no
# fleet binding to reach.
#
# Absent means 404, which is the right answer for a self-hosted fleet: somebody
# else's private coordinator has no product page to point at. Ours does, so it
# is set here and in no fork.
AGENT_FLEET_DOCS_URL = "https://fleetdemo.thetech.network/docs"

# --- the GitHub App -----------------------------------------------------------
# Identifiers, not credentials, and that is why they are here rather than in a
# secret. A client id appears in the query string of every authorize URL the
# app hands out — it is public by construction, and pretending otherwise would
# be the shared-secret habit wearing a different hat.
#
# The CLIENT SECRET is a secret and lives in `wrangler secret put`:
#
#   wrangler secret put AGENT_FLEET_GITHUB_CLIENT_SECRET
#
# It is worth being exact about what that secret is and is not. On its own it
# authorises nothing — it cannot read a repository, mint a token, or identify
# anybody. It is only useful alongside an authorization code the person just
# produced, or a refresh token they already granted. That is what makes it
# tolerable in a coordinator, when the App's PRIVATE KEY is not: the private
# key mints installation tokens for every installation of the App, which is
# every member and every org that ever installed it. See docs/github-app.md.
#
# Absent means "no App configured", which is not an error: the paste route is a
# first-class answer, not a fallback, and a fresh clone of this repo has no App
# and should still work.
AGENT_FLEET_GITHUB_CLIENT_ID = "Iv23liR4EwdP1xDxLt5E"

# Kept because installation tokens will want it — though GitHub now accepts the
# CLIENT ID as the JWT issuer in its place, so this may turn out to be the last
# thing that needs it. Recorded rather than dropped: an id that is hard to find
# again costs more than a line here.
AGENT_FLEET_GITHUB_APP_ID = "4758006"

# The slug, which is what the INSTALL url is built from:
#
#   https://github.com/apps/fleetwright-agents/installations/new
#
# Not derivable from the name without guessing at GitHub's slugifying — "Fleetwright
# Agents" became `fleetwright-agents`, and the OAuth path needs no slug at all,
# so this exists purely so the install link can be built without a lookup that
# requires the private key to authenticate.
AGENT_FLEET_GITHUB_APP_SLUG = "fleetwright-agents"


# --- who is allowed to sign in ----------------------------------------------
# Identifiers, not credentials: the issuer is public, the audience is the client
# id an app ships anyway, and the allowlist is a list of colleagues. Keeping
# them here means changing who can reach a fleet is a reviewable diff rather
# than a value somebody edited in a web form at midnight.
#
# Empty ALLOW means nobody, deliberately — see src/fleet/coordinator/oidc.js.
AGENT_FLEET_AUTH_ISSUERS = "https://accounts.google.com,https://appleid.apple.com"

# TWO audiences, because the two providers issue for different things. Apple
# issues for the iOS BUNDLE ID; Google issues for the OAuth WEB client id
# (client_type 3 in google-services.json) that the Android app names as its
# server client — NOT for either of the Android client ids sitting next to it
# in that file, which are what the request is authorised by rather than what
# the token is issued for. `aud` is what gets verified.
AGENT_FLEET_AUTH_AUDIENCES = "network.thetech.fleetwright,654943059314-kosvngt4ggmdguksogppoiglo48nvm2i.apps.googleusercontent.com"

# Matched against the VERIFIED EMAIL in the ID token, so it must be the address
# actually signed in with, not the one that ought to work. A bare `@domain`
# entry matches the whole domain; anything else must match in full.
#
# `@thetech.network` alone would refuse the person who owns this fleet — the
# Google account here is a gmail one, and the refusal reads "… is not on this
# fleet's list", which is accurate and sounds like a bug. Sign in with Apple
# will present whatever address that Apple ID uses, which may be a third one
# again; add it when it is known rather than guessing at it now.
#
# THE TWO GUESTS. Added now because the thing that made a guest safe finally
# exists: a member sees only the sessions their own verified identity created
# (#182), and brings their own Claude, GitHub and Cloudflare credentials rather
# than inheriting anybody's (#175, #181). Before those, "invite somebody" meant
# handing over the fleet.
#
# Whole addresses, never a domain: gmail.com as a domain entry would allow
# every Google account on earth.
AGENT_FLEET_AUTH_ALLOW = "@thetech.network,elibrody2@gmail.com,reservedjyumi@gmail.com,e6591050@gmail.com"

# Inviting somebody by email. BOTH ARE OPTIONAL and the invitation works
# without them — the list is the authority and the mail is a courtesy, so a
# deployment with neither set simply reports that no email was sent and the
# person doing the inviting sends the app themselves.
#
#   AGENT_FLEET_INVITE_FROM   an address on a domain THIS ACCOUNT CONTROLS.
#                             Cloudflare refuses anything else, and that
#                             refusal is surfaced to whoever tapped Invite
#                             rather than left in a log.
#   AGENT_FLEET_NAME          what to call this fleet in the message
#   AGENT_FLEET_APP_IOS       where to get the app, per phone. Two values and
#   AGENT_FLEET_APP_ANDROID   not one, because the recipient's phone decides
#                             which link is the real one and an invitation is
#                             the one message that cannot know that in advance.
#                             Either may be left unset; with neither, the
#                             message says so rather than arriving with no way
#                             to act on it.
#
# The `send_email` binding below is what actually sends. Email Sending is
# enabled on this account and the plan covers arbitrary recipients, so an
# invitation reaches somebody who has never been near this fleet — which is the
# only kind of person worth emailing an invitation to.
#
# THE SENDER MUST BE AN ADDRESS THIS ACCOUNT CAN SEND AS — a domain set up for
# Email Sending. It is the coordinator's own domain, which is worth having: an
# invitation arriving from the address the fleet already answers on is one the
# recipient can check, and one fewer domain for somebody to wonder about.
#
# `invites@` rather than `noreply@`: nobody replies to either, and only one of
# them says what it is.
#
# Cloudflare refuses a sender this account cannot send as, and that refusal is
# surfaced to whoever tapped Invite rather than left in a log — so a wrong value
# here costs one confusing invitation and not a silent hole.
AGENT_FLEET_NAME = "the Fleetwright fleet"
AGENT_FLEET_INVITE_FROM = "invites@fleet.thetech.network"

# ERROR REPORTING. A DSN is not a secret — it identifies a project and grants
# only the ability to send it events, which is why it lives here as a var rather
# than in `wrangler secret`. Unset means no reporting at all: a fresh clone, a
# contributor's `wrangler dev` and a self-hosted fleet post nowhere, and turning
# it off in an incident is a var change rather than a code deploy.
#
# WHAT IT MAY SEND IS DECIDED IN src/sentry.js, and the answer is: not the URL's
# query, not a header, not a body. This coordinator carries a device credential
# on nearly every request — one of them in the query string, deliberately, for
# Shortcuts.
SENTRY_DSN = "https://08d0e1e893370804857f8ce9e2f39f17@o4512012096765952.ingest.us.sentry.io/4512012108955648"
SENTRY_ENVIRONMENT = "production"
# Not 1.0. Every host sends a health frame every fifteen seconds and every phone
# polls; tracing all of it spends the quota an actual incident needs.
SENTRY_TRACES_SAMPLE_RATE = "0.05"
# Where to get the app. iOS is a TestFlight beta rather than the App Store, and
# the invitation says so on its own — a TestFlight link needs Apple's TestFlight
# app installed first and only opens on the device, which is invisible to anyone
# who has not used one before.
AGENT_FLEET_APP_IOS = "https://testflight.apple.com/join/FyQnP8T9"
AGENT_FLEET_APP_ANDROID = "https://play.google.com/store/apps/details?id=network.thetech.fleetwright"

[[send_email]]
name = "EMAIL"
# `remote = true` so a local `wrangler dev` sends through the real service
# rather than failing on a binding the local runtime cannot provide. Sending an
# invitation is not a thing to discover works only in production.
remote = true

# Signing in is the other route reachable without a credential, and it does real
# work for an anonymous caller: fetch a key set, verify a JWT, and on success
# write a client record. Ten a minute per address is more than a person tapping
# a button and less than anything worth doing with somebody else's CPU.
#
# Namespace 1002 rather than 1001, which the demo Worker uses. The budgets
# cannot be shared now that the scripts are not — a script hammering the demo
# fleet reaches a different Worker entirely — but the numbers stay distinct so
# nothing depends on that being remembered.
[[ratelimits]]
name = "SIGNIN_RATE_LIMIT"
namespace_id = "1002"
simple = { limit = 10, period = 60 }

[[durable_objects.bindings]]
name = "FLEET"
class_name = "Fleet"

# One DO instance holds every host socket and the device registry, so there is
# exactly one place that knows the fleet. A fleet is tens of hosts, not
# thousands — sharding this would add a consistency problem to buy headroom
# nobody needs.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["Fleet"]

[observability]
enabled = true

# --- secrets, set with `wrangler secret put NAME` ----------------------------
#
#   AGENT_FLEET_API_TOKEN             break-glass admin. NOT what phones use
#   AGENT_FLEET_AUTH_ISSUERS          who may vouch for a person, e.g.
#                                     https://appleid.apple.com https://accounts.google.com
#   AGENT_FLEET_AUTH_AUDIENCES        which app the ID token must be for — the
#                                     iOS bundle id, the Android web client id
#   AGENT_FLEET_AUTH_ALLOW            who is allowed in: whole addresses, or
#                                     @domain. EMPTY ALLOWS NOBODY, on purpose
#
# THERE IS NO HOST TOKEN. Hosts keep a keypair and sign a nonce per connection;
# they enrol with a six-digit pin minted by somebody already in. AGENT_FLEET_HOST_TOKEN
# was one string every machine presented — indistinguishable hosts, no way to
# revoke one, replayable by anything that saw a single connection.
#   AGENT_FLEET_FCM_SERVICE_ACCOUNT   the Firebase service-account JSON (or
#                                     base64 of it), for push
#   AGENT_FLEET_APNS_KEY              the .p8, for push to iOS
#   AGENT_FLEET_APNS_KEY_ID           which key that is
#   AGENT_FLEET_APNS_TEAM_ID          the team it belongs to
#
# The key id and team id are identifiers rather than credentials, and were
# briefly a [vars] entry for that reason. They are secrets now because a name
# cannot be both: Cloudflare keeps vars and secrets in one namespace, so a
# deploy carrying a var CLOBBERS the secret of the same name. One place per
# name, and for these the useful place is beside the .p8 they describe.
#
# AGENT_FLEET_DEMO_TOKEN is not here and is not in this file at all any more:
# it belongs to wrangler.demo.toml, along with the fleet it describes.
#
# Without AGENT_FLEET_API_TOKEN the Worker refuses every request rather than
# running open — see worker.js. Without the AUTH_ pair, sign-in answers 503 and
# says so, which leaves the admin token as the only way in. Without the FCM
# service account, push is logged instead of sent, and says so.
