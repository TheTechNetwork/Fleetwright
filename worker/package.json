{
  "name": "agent-fleet-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "node --test test/"
  },
  "devDependencies": {
    "wrangler": "4.128.0"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@sentry/cloudflare": "10.73.0"
  }
}
