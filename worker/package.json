{
  "name": "agent-fleet-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "node --test test/"
  },
  "devDependencies": {
    "wrangler": "4.127.1"
  },
  "engines": {
    "node": ">=24"
  }
}
