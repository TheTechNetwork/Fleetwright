{
  "name": "agent-fleet-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "node --test test/"
  },
  "devDependencies": {
    "wrangler": "4.136.1"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@sentry/cloudflare": "10.75.0"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
