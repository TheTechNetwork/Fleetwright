{
  "name": "agent-fleet-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "test": "node --test test/"
  },
  "devDependencies": {
    "wrangler": "4.130.0"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@sentry/cloudflare": "10.74.0"
  },
  "overrides": {
    "sharp": "0.35.4"
  }
}
