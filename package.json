{
  "name": "agent-fleet",
  "version": "0.0.1",
  "description": "Multi-host control plane for Claude Code sessions — coordinator, scheduler, sandbox and mobile API.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test test/*.test.js",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.0"
  }
}
