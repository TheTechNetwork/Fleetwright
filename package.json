{
  "name": "agent-fleet",
  "version": "0.0.1",
  "description": "Multi-host control plane for Claude Code sessions — coordinator, scheduler, sandbox and mobile API.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "npm run test:fleet && npm run test:hub",
    "test:fleet": "node --test test/*.test.js",
    "test:hub": "node --test agent-hub/test/*.test.js",
    "typecheck": "npm run typecheck:fleet && npm run typecheck:hub",
    "typecheck:fleet": "tsc -p tsconfig.json",
    "typecheck:hub": "tsc -p agent-hub/tsconfig.json",
    "sidecar": "node bin/agent-fleet-sidecar",
    "hub": "node agent-hub/src/index.js"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "typescript": "^5.6.0"
  }
}
